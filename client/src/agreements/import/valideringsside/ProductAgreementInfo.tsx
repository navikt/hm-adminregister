import { ProductAgreementRegistrationDTO } from "utils/types/response-types";

export const ProductAgreementInfo = ({ productAgreement }: { productAgreement: ProductAgreementRegistrationDTO }) => {
  return (
    <div className="product-series-info">
      <p></p>
    </div>
  );
};
